[phases.setup]
nixPkgs = ["python311", "gcc", "postgresql15"]

[phases.install]
dependsOn = ["setup"]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"